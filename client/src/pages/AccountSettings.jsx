import React from 'react';
import { Typography } from '@mui/material';

const AccountSettings = () => {
  return <Typography variant="h4">Account Settings (Coming soon)</Typography>;
};

export default AccountSettings; 